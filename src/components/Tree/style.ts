import styled from "styled-components";

export const TreeItemStyle = styled.div`
    .tree-item-container {
        width: 100%;
        padding: 3px 0px;
        height: auto;
        // overflow: hidden;
        .current-content {
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 10px;
        }
    }
`;
